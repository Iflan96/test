# Default values for mychart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: iflan7744/jenkins-pipeline
  tag: latest
  pullPolicy: IfNotPresent
service:
  type: NodePort

